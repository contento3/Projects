ALTER TABLE `crm`.`users` ADD COLUMN `SALT` VARCHAR(45) NOT NULL  AFTER `IS_EMAIL_USERNAME` ;

ALTER TABLE `crm`.`users` CHANGE COLUMN `USERNAME` `USERNAME` VARCHAR(256) NOT NULL  ;


ALTER TABLE `crm`.`users` ADD COLUMN `FIRST_NAME` VARCHAR(250) NOT NULL  AFTER `SALT` , ADD COLUMN `LAST_NAME` VARCHAR(250) NOT NULL  AFTER `FIRST_NAME` , ADD COLUMN `MIDDLE_NAME` VARCHAR(250) NULL  AFTER `LAST_NAME` ;