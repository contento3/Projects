CREATE  TABLE `modules`.`MODULE_CATEGORY` (
  `MODULE_CATEGORY_ID` INT(11) NOT NULL AUTO_INCREMENT,
  `CATEGORY_NAME` VARCHAR(250) NOT NULL ,
  `CATEGORY_DESCRIPTION` VARCHAR(500) NULL ,
  PRIMARY KEY (`MODULE_CATEGORY_ID`) );
