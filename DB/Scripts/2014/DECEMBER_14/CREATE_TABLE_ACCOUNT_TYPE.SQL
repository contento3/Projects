CREATE  TABLE `modules`.`ACCOUNT_TYPE` (
  `ACCOUNT_TYPE_ID` INT(11) NOT NULL AUTO_INCREMENT,
  `ACCOUNT_TYPE_NAME` VARCHAR(250) NOT NULL ,
  `DESCRIPTION` VARCHAR(5000) NULL ,
  PRIMARY KEY (`ACCOUNT_TYPE_ID`) );
